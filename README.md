Banco de Dados 

CREATE DATABASE formb;

USE formb;

CREATE TABLE Users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    sunname VARCHAR(255),
    setor VARCHAR(255),
    usersname VARCHAR(255),
    password VARCHAR(255)
);
